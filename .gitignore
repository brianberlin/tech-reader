.DS_Store

# Rust
/target/
**/*.rs.bk

# Dev artifacts: voice-model cache + rendered audio / logs (never commit large .onnx)
/voices/
/out/
/dist/
# Ignore the downloaded onnxruntime archive, but keep its committed sha256 pin.
/build/onnxruntime-*
!/build/onnxruntime-*.sha256
*.onnx
*.onnx.json
